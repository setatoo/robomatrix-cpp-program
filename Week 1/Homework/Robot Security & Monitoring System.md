# 🔥 تمرین اختیاری ویژه (سطح بسیار پیشرفته)
## 🧠 Robot Security & Monitoring System

> ⚠️ این تمرین کاملاً اختیاری است.  
> فقط برای دانشجویانی که می‌خواهند چالش واقعی تجربه کنند.

---

## 🎯 هدف تمرین

طراحی یک سیستم امنیتی و مانیتورینگ برای یک ربات که:

- به‌صورت منومحور اجرا شود
- وضعیت داخلی سیستم (State) را نگه دارد
- چندین شرط تو در تو داشته باشد
- رفتار برنامه وابسته به تاریخچه ورودی‌ها باشد

---

## 🛠 مشخصات برنامه

برنامه باید یک منو نمایش دهد:

1. ورود کد دسترسی
2. ثبت سرعت جدید
3. نمایش گزارش
4. ریست سیستم
5. خروج

---

## ⚙️ قوانین سیستم

### 🔐 1️⃣ ورود کد دسترسی

- کاربر باید یک عدد 4 رقمی وارد کند.
- کد صحیح برابر است با: `2024`
- کاربر فقط **3 بار فرصت** دارد.
- اگر 3 بار اشتباه وارد کند:
  - سیستم قفل شود.
  - تا زمانی که ریست انجام نشود هیچ عملیاتی اجازه داده نشود.

---

### 🚀 2️⃣ ثبت سرعت جدید

فقط در صورتی مجاز است که سیستم قفل نباشد.

- کاربر یک سرعت وارد می‌کند.
- اگر سرعت > 120 → هشدار خطر ثبت شود.
- اگر سرعت > 200 → سیستم وارد وضعیت بحرانی شود.
- اگر 3 هشدار خطر پشت سر هم ثبت شود → سیستم قفل شود.

سیستم باید موارد زیر را ذخیره کند:
- بیشترین سرعت ثبت‌شده
- تعداد کل هشدارها
- تعداد دفعات ورود به وضعیت بحرانی

---

### 📊 3️⃣ نمایش گزارش

باید موارد زیر نمایش داده شود:

- وضعیت سیستم (فعال / قفل‌شده)
- بیشترین سرعت ثبت‌شده
- تعداد هشدارها
- تعداد وضعیت‌های بحرانی

---

### ♻️ 4️⃣ ریست سیستم

- قفل را باز کند
- شمارنده خطاهای ورود را صفر کند
- هشدارهای متوالی را صفر کند
- اما بیشترین سرعت و آمار کلی حذف نشود

---

### 🚪 5️⃣ خروج

- برنامه فقط زمانی متوقف شود که کاربر گزینه 5 را انتخاب کند.

---

## 📌 الزامات فنی

- فقط از `if / else if / else` استفاده شود.
- فقط از `while` برای تکرار استفاده شود.
- استفاده از `break` مجاز نیست.
- استفاده از `switch` مجاز نیست.
- استفاده از آرایه یا تابع مجاز نیست.
- تمام وضعیت‌ها باید با متغیرها مدیریت شوند.

---

## 🧠 نکات مهم طراحی

برای حل این تمرین باید بتوانید:

- چندین متغیر وضعیت تعریف کنید (State Variables)
- شرط‌های تو در تو طراحی کنید
- منطق وابسته به تاریخچه (مثل 3 هشدار پشت سر هم) پیاده‌سازی کنید
- بین «قفل بودن» و «فعال بودن» سیستم تفاوت قائل شوید

---

## 🏆 چالش واقعی

اگر بتوانید طوری طراحی کنید که:

- وقتی سیستم قفل است فقط گزینه‌های 1 (ورود کد) و 4 (ریست) کار کنند
- بقیه گزینه‌ها پیام خطای منطقی بدهند

شما عملاً یک State Machine ساده طراحی کرده‌اید.

---

# 🎖 سطح سختی: بسیار بالا

این تمرین فقط با مفاهیم جلسه اول قابل حل است،  
اما نیاز به تفکر مهندسی عمیق دارد.

اگر بتوانید آن را کامل و بدون باگ پیاده‌سازی کنید،  
یعنی منطق برنامه‌نویسی را واقعاً درک کرده‌اید.
