# 🧮 Homework 01 – Interactive Calculator Menu

## 📌 توضیحات (فارسی)

### 🎯 هدف تمرین
در این تمرین باید یک ماشین حساب تعاملی طراحی کنید که با استفاده از حلقه `while` و ساختارهای شرطی کار کند.

---

## 🛠 مشخصات برنامه

برنامه باید:

1. یک منو به کاربر نمایش دهد:

Addition

Subtraction

Multiplication

Division

Exit


2. از کاربر یک گزینه دریافت کند.
3. اگر گزینه بین 1 تا 4 بود:
   - دو عدد از کاربر بگیرد
   - عملیات مربوطه را انجام دهد
   - نتیجه را نمایش دهد
4. اگر گزینه 5 بود:
   - برنامه پایان یابد.
5. اگر ورودی نامعتبر بود:
   - پیام خطا نمایش دهد
   - دوباره منو را نمایش دهد.

---

## ⚙️ الزامات فنی

- حتماً از `while` برای تکرار منو استفاده شود.
- برنامه باید تا زمانی که کاربر گزینه 5 را انتخاب نکرده ادامه پیدا کند.
- تقسیم بر صفر باید مدیریت شود.
- از `namespace std` استفاده نکنید (از `std::` استفاده کنید).
- کد باید خوانا و مرتب باشد.

---

## 🚫 محدودیت‌ها

- استفاده از `break` مجاز نیست.
- استفاده از `switch` مجاز نیست (فقط if / else if / else).
- استفاده از توابع در این تمرین مجاز نیست.

---

## 📤 نحوه تحویل

- فایل را با نام `homework01.cpp` ذخیره کنید.
- پروژه را در GitHub خود آپلود کنید.
- سورس کد باید کامپایل شود و بدون خطا اجرا شود.

---

---

# 🧮 Homework 01 – Interactive Calculator Menu

## 📌 Description (English)

### 🎯 Objective
In this assignment, you must design an interactive calculator using a `while` loop and conditional statements.

---

## 🛠 Program Requirements

The program must:

1. Display the following menu:

Addition

Subtraction

Multiplication

Division

Exit


2. Receive a choice from the user.
3. If the choice is between 1 and 4:
   - Ask for two numbers
   - Perform the selected operation
   - Display the result
4. If the choice is 5:
   - Terminate the program.
5. If the input is invalid:
   - Display an error message
   - Show the menu again.

---

## ⚙️ Technical Requirements

- You must use a `while` loop to repeat the menu.
- The program must continue running until the user selects option 5.
- Division by zero must be handled properly.
- Do not use `using namespace std;`
- Code must be clean and readable.

---

## 🚫 Restrictions

- Using `break` is NOT allowed.
- Using `switch` is NOT allowed (only if / else if / else).
- Functions are NOT allowed in this assignment.

---

## 📤 Submission

- Save the file as `homework01.cpp`
- Upload it to your GitHub repository.
- The program must compile and run without errors.
